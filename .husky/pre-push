#!/bin/sh

if [ -z "${PUBLIC_SITE_URL:-}" ] || [ -z "${PUBLIC_BASE_PATH+x}" ]; then
  if [ -f .env ]; then
    set -a
    . ./.env
    set +a
  else
    echo "Missing PUBLIC_SITE_URL/PUBLIC_BASE_PATH. Copy .env.example to .env." >&2
    exit 1
  fi
fi

npm run verify
